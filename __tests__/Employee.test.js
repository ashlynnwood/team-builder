// Employee is parent class

// const { default: test } = require("node:test") 

// require Employee class

// write a test to see if you can initiate an Employee instance
test("Can instantiate Employee instance", () => {
    // variable for the new Employee
    // expect the type of that variable to be an object
});

// write a test to see if you can set the name as a constructor argument
test("Can set name via constructor arguments", () => {
  // variable for name
  // variable for a new Employee with that name passed in as an argument
  // expect the variable.name to be the variable that you set up for name
});

// same as above for id

// same as above for email

// write a test to see if your getter method is working
test("Can get name via getName()", () => {
  // get Name method should return name of employee
  // variable for the test value
  // variable for the new Employee with that test value passed in
  // expect that when you call that method - to be the test value var
});

// same for getting the id

// same for getting the email

// same for getting the role